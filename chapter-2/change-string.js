//BROKEN CODE
//This example shows us how JavaScript will allow us to write code to change the value of the string but the is ignores that line and retains the initial value.
const greet = 'dude';
console.log(greet);

greet[0] = 'r';
console.log(greet);
