'use-strict';
//For simple iterations, the enhanced for loop is a better choice. It requires less typing and produces the same result while being easier to read.
//Since this code is consice, it is less error prone.
const names = ['Sara', 'Jake', 'Pete', 'Mark', 'Jill'];

for(const name of names) {
    console.log(name);
}
